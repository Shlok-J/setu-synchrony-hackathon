# Copy to .env locally (never commit the real .env) or set these as
# Codespaces secrets instead (Settings -> Secrets and variables -> Codespaces).

# Standing in for "AWS Bedrock or equivalent" per the problem statement's
# own wording. Leave unset and the model-service falls back to a
# deterministic template explanation -- the demo never breaks either way.
ANTHROPIC_API_KEY=

POSTGRES_PASSWORD=setu_dev_password
MODEL_SERVICE_URL=http://localhost:8000
