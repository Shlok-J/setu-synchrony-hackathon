# copy to .env locally, or set these as Codespaces secrets instead

# free key: https://console.groq.com/keys -- leave blank and
# explanations fall back to a template instead of breaking
GROQ_API_KEY=
GROQ_MODEL=qwen/qwen3.8-27b

POSTGRES_PASSWORD=setu_dev_password
MODEL_SERVICE_URL=http://localhost:8000

# blank = API auth disabled
APP_API_KEY=
